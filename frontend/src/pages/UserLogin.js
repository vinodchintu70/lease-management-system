import React,{useState}from"react";import axios from"axios";import{Link,useNavigate}from"react-router-dom";
export default function UserLogin(){const[e,setE]=useState(""),[p,setP]=useState(""),n=useNavigate();const f=async x=>{x.preventDefault();try{const r=await axios.post("http://localhost:5000/users/login",{email:e,password:p});localStorage.setItem("user",JSON.stringify(r.data.user));n("/user-dashboard")}catch(x){alert(x.response?.data?.message||"Login failed")}};return <div className="login"><form onSubmit={f}><h1>User Login</h1><input placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)}/><button>Login</button><p><Link to="/register">Register</Link> | <Link to="/admin-login">Admin Login</Link></p></form></div>}